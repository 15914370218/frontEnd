reduce()方法  可以去重累加 减少
  var arr=[1,2,3,4,5];
  //参数 传入一个函数
  //第一个参数  p代表上一次运算的结果  prev  一开始就是索引为0的那个元素
  //第二个参数  代表当次运算的元素
  //第三个参数 代表索引  当前参与运算的索引  代表第二个参数的元素对应数组的索引  基本都是从1开始
  //最后一个参数 代表当前数组
  //10代表初始值 如果没有提供初始值，则将使用数组中的第一个元素
  //Accumulator累加器
  arr.reduce(function(p,ele,i,arr){
    console.log(p,ele,i,arr);
    return p+ele
  }，10);

fill()  填充数组  [].fill(1),数组中每个元素都是1
